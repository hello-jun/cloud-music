import React from 'react'
import PropTypes from 'prop-types'

const Rank = props => {
    return (
        <div>
            Rank
        </div>
    )
}

Rank.propTypes = {

}

export default React.memo(Rank) 
